# Define the string variable
name = 'Python'

# Use len() to get the total number of elements in the string
print(len(name))  

# Use enumerate to iterate through each element in the string
for i in enumerate(name):
    print(i)
